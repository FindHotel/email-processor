test:
  pre:
      - pip install -r requirements-dev.txt
  post:
    - git submodule sync
    - git submodule update --init
checkout:
  post:
    - git submodule sync
    - git submodule update --init
